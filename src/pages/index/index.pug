extends ../../app/pug/layout

block title
    | Главная

block content
    main(data-barba="container" data-barba-namespace="common" data-page-namespace="index")
        +section-hero
            +block-hero-slider
        +section-main({ heading: dataJson.index.blockHeading.company })
            +block-statistic(dataJson.index.blockStatistic)
        +section-main({ heading: dataJson.index.blockHeading.project })
            +block-project(dataJson.index.blockProject)
        +section-about(dataJson.index.sectionAbout)
        +section-main({ heading: dataJson.index.blockHeading.media })
            +block-media(dataJson.index.blockMedia)
