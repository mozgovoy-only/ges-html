include ./mixins
include ./components

<!DOCTYPE html>
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title
            block title

    body
        +header(dataJson.header)
        .layout-main
            .layout-main__content(data-barba="wrapper")
                block content
            +footer(dataJson.footer)
