extends ../../app/pug/layout

block title
    | Южный речной вокзал получил новую жизнь после реконструкции

block content
    div(data-barba="container" data-barba-namespace="common" data-page-namespace="press-center-view")
        +section-hero(dataJson.pressCenterView.sectionHero)
        +section-main({ contentClassList: 'section-main__content--inner' })
            .wysiwyg!=dataJson.pressCenterView.sectionMain.content.wysiwyg1
            +slider-gallery()
        +section-main(dataJson.pressCenterView.sectionMain.media)
            +block-media(dataJson.pressCenterView.blockMedia)
