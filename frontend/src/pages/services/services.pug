extends ../../app/pug/layout

block title
    | Услуги

block content
    div(data-barba="container" data-barba-namespace="common" data-page-namespace="services")
        +section-hero(dataJson.services.sectionHero)
        +section-main(dataJson.services.sectionMain.generalContracting)
            +slider(dataJson.services.sliders.generalContracting)
        +section-main(dataJson.services.sectionMain.generalProjects)
            +slider(dataJson.services.sliders.generalProjects)
