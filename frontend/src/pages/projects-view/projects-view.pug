extends ../../app/pug/layout

block title
    | ЖК «Преображенская площадь» 1 очередь

block content
    div(data-barba="container" data-barba-namespace="common" data-page-namespace="projects-view")
        +section-projects-hero(dataJson.projectsView.sectionProjectsHero)
        +section-main(dataJson.projectsView.sectionMain.projectsAbout)
        +section-main(dataJson.projectsView.sectionMain.location)
            +geo-projects()
        +section-stages(dataJson.projectsView.sectionStages)
        +section-main(dataJson.projectsView.sectionMain.media)
            +slider-gallery()
        +section-main(dataJson.projectsView.sectionMain.aboutUs)
        +section-main(dataJson.projectsView.sectionMain.mediaAbout)
            +block-media(dataJson.projectsView.blockMedia)
        +section-main({})
            +card-next(dataJson.projectsView.cardNext)

