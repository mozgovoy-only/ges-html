mixin header(props)
    header&attributes(attributes).header
        .header__content
            a(href="/").header__logo
                span.header__logo-inner
                    +svg('logo')
            .header__menu
                +header-menu()
            .header__button-menu
                +button-menu()
