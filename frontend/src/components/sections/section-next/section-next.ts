import Component, { ComponentProps } from '@/base/component';

type SectionNextOptions = {

}

export default class SectionNext extends Component<HTMLElement, SectionNextOptions> {
    constructor(element: ComponentProps<HTMLElement>) {
        super(element);
    }

    destroy() {
        // Destroy function
    }
}
