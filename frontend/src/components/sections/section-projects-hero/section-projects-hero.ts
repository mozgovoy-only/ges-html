import Component, { ComponentProps } from '@/base/component';

type SectionProjectsHeroOptions = {

}

export default class SectionProjectsHero extends Component<HTMLElement, SectionProjectsHeroOptions> {
    constructor(element: ComponentProps<HTMLElement>) {
        super(element);
    }

    destroy() {
        // Destroy function
    }
}
