import Component, { ComponentProps } from '@/base/component';

type SectionStagesOptions = {

}

export default class SectionStages extends Component<HTMLElement, SectionStagesOptions> {
    constructor(element: ComponentProps<HTMLElement>) {
        super(element);
    }

    destroy() {
        // Destroy function
    }
}
