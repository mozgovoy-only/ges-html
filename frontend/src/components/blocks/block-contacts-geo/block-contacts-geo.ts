import Component, { ComponentProps } from '@/base/component';

type BlockContactsGeoOptions = {

}

export default class BlockContactsGeo extends Component<HTMLElement, BlockContactsGeoOptions> {
    constructor(element: ComponentProps<HTMLElement>) {
        super(element);
    }

    destroy() {
        // Destroy function
    }
}
